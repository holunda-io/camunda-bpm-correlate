spring:
  cloud:
    stream:
      bindings:
      function:
        definition: directIngressMessageConsumer
        bindings:
          directIngressMessageConsumer-in-0: correlate-ingres
correlate:
  enabled: false
