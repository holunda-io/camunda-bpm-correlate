spring:
  cloud:
    stream:
      function:
        definition: directIngressMessageConsumer
        bindings:
          directIngressMessageConsumer-in-0: correlate-ingress-binding
correlate:
  enabled: false
